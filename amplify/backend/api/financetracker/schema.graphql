enum TYPE {
  expense
  income
}

type Item
  @model
  @auth(rules: [{ allow: owner }])
  @key(fields: ["PK", "SK"])
{
  PK: String!
  SK: String!
  user_uuid: String!
  type: TYPE!
  category: String!
  amount: Float!
  date: AWSDate!
  note: String
}
